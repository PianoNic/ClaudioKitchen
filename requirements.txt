# Runtime dependencies for ClaudioKitchen (server.py).
# fastmcp bundles Starlette/Uvicorn; httpx is used for all OpenRouter calls.
# Pinned to the fastmcp 3.x line (verified end-to-end on 3.4.4); the upper bound
# stops a future major from silently landing in a rebuild. Run `pip freeze` in the
# built image if you want a full lock.
fastmcp>=3.4,<4
httpx>=0.27,<1
