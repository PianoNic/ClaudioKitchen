# Runtime dependencies for ClaudioKitchen (server.py).
# fastmcp bundles Starlette/Uvicorn; httpx is used for all OpenRouter calls.
# Lower bounds only; run `pip freeze` in the built image if you want a full lock.
fastmcp>=2.13.2
httpx>=0.27
